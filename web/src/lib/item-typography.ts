/** Item card title — matches list row / board item headline size (inherits rem root). */
export const ITEM_HEADLINE_CLASS =
  "text-right text-sm font-semibold leading-tight text-slate-900";

/** Item body — same size as headline, lighter weight. */
export const ITEM_BODY_CLASS =
  "text-sm font-normal leading-tight text-slate-600";

export const ITEM_BODY_SECONDARY_CLASS =
  "text-sm font-normal leading-tight text-slate-400";

/** List card title inside task lists modal. */
export const TASK_LIST_TITLE_CLASS = "text-sm font-semibold text-slate-900";
